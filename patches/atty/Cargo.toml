# In-tree replacement for crates.io `atty` (unmaintained; uses unsound Windows path).
# Same name/version so `[patch.crates-io]` replaces the registry crate; implementation uses `std::io::IsTerminal`.
[package]
name = "atty"
version = "0.2.14"
edition = "2021"
publish = false
